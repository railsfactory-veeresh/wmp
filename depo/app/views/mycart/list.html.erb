
<h1>your current products are</h1>
<table>
	 <thread>
	 	<tr>
	 <th>product</th>
	 <th>price</th>
	 
	   <th colspan="3"></th>
    </tr>
	</thread>

<tbody>
 <% @mycart.each do |item| %><br/>

      <tr>
        <td><%=item.product.title%></td>
        <td><%=number_to_currency(item.product.price) %></td>
        
     
      </tr>
    <% end %>
  </tbody>
</table>

 <%= link_to 'continu_shopping', '/' %>